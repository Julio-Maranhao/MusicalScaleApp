import { DraggableHostDirective } from './draggable-host.directive';

describe('DraggableHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableHostDirective();
    expect(directive).toBeTruthy();
  });
});
